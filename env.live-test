# env.live-test - Configuration for live-test development environment
# Redis URL is set via docker-compose environment override

REDIS_PREFIX=lcm-dev
REDIS_SOCKET_TIMEOUT=2
REDIS_CONNECT_TIMEOUT=2

# Use Redis for storage in test environment
STORAGE_PROVIDER=MEMORY

# Verbose logging for development
LOG_REQUESTS=1
LOG_REQUEST_HEADERS=1
LOG_REQUEST_BODY=1
LOG_BODY_MAX=8192
LOG_HEADER_ALLOWLIST="content-type,content-length,x-forwarded-for,x-real-ip,user-agent,host"
LOG_PATH_PREFIXES="/generate,/superres,/sdapi,/dream"

# Server config
PORT=4200
NUM_WORKERS=1
QUEUE_MAX=4

# Model paths (mounted volume)
MODEL_ROOT=/models

# Super-resolution (disabled by default for faster testing)
SR_NUM_WORKERS=1
SR_QUEUE_MAX=4
SR_ENABLED=0

# Backend auto-detection (cpu fallback for dev without GPU/NPU)
BACKEND=AUTO
